# climate-challenge-week0
## Environment Setup
To reproduce the development environment, ensure you have [Conda](https://docs.conda.io/en/latest/) installed and run:

1. **Create the environment from the file:**
   ```bash
   conda env create -f environment.yml 